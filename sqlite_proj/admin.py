from django.contrib import admin
from.models import MyInfo    # Here import the class name of the models used "Datas"

admin.site.register(MyInfo)   # Here Register the models to store the values into database
# Register your models here.
